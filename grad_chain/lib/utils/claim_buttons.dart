//import 'package:flutter/material.dart';

/*
  String _buttonText = 'Click to Claim';

  _changeToClaimed() {
    setState(() {
      _buttonText = 'Claimed!';
    });
  }

  @override
  Widget build(BuildContext context) {
    return Center(
        child: ElevatedButton(
      onPressed: _changeToClaimed(),
      child: Text(_buttonText),
    ));
  }
}
*/
